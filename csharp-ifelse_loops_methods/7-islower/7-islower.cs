using System;

class Character
{
    public static bool IsLower(char c)
    {
        // Check if the character is in the range of lowercase letters 'a' to 'z'
        return c >= 'a' && c <= 'z';
    }
}
